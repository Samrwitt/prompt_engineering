# Paper-only scripts

These files inflate sample size or inject synthetic curves. They are **not** used for the README, dashboard, or GitHub Pages numbers.

Use `python -m src stats --path results/benchmark_mock/runs.jsonl` instead.
